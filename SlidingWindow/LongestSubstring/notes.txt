Longest Substring Without Repeating Characters

We will need to utilize the concept of sliding window for this one, to initialize a sliding window
is also the same as initializing a two pointers where we utilize its concept but imagine it as a sliding
window